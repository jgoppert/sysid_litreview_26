# Inverse PINN

Planned inverse physics-informed neural-network estimator. This should be
added after the shared dataset and metrics are fixed because training cost and
loss weighting will otherwise obscure method comparisons.

Target outputs:

- data loss, physics loss, and total loss history
- trajectory fit
- parameter error
- runtime/training cost

